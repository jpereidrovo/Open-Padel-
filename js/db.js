// db.js – base de jugadores (placeholder)
document.addEventListener("DOMContentLoaded", () => {
  const mount = document.getElementById("baseMount");
  if (mount) {
    mount.innerHTML = "<p>📋 Base de jugadores cargada (db.js)</p>";
  }
});
